name: Automated assignment check
run-name: testing out GitHub Actions 🚀
on:
  pull_request:
    types:
      - opened
    branches:
      - master
  push:
    branches:
      - homework/**
jobs:
  Check-Homework:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing..."